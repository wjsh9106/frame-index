export default {
  // state.app：app模块下的state
  // 顶部导航选中菜单id
  headerMenuActiveIndex: (state) => state.app.headerMenuActiveIndex,
  // 顶部导航选中菜单name
  headerMenuActiveName: (state) => state.app.headerMenuActiveName
}
